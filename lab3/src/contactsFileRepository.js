const crypto = require('crypto')
const fs = require('fs')
const path = require('path')

const repo = {
  findAll: ,
  findById: ,
  create: newContact => {
    
  },
  deleteById: id => {
   
  },
  update: contact => {
    
  }
}

loadData()

module.exports = repo