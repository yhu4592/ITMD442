extends layout

block content
  h1= title
  hr
  p= info.email
  p= info.notes
  p= info.date
  a(href=`/contacts/${info.contactID}/edit`) Edit Contact
  | 
  a(href=`/contacts/${info.contactID}/delete`) Delete Contact